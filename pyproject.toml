[project]
name = "at-node"
version = "1.0.0"
description = "CH582F AT-Node host-side test tools (AT command, HID key injection, dongle loop)"
requires-python = ">=3.10"
dependencies = [
    "pyserial>=3.5",
]

[tool.uv]
package = false
