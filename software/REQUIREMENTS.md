# at-node 需求文档

> 版本：v0.1 · 最后更新：2026-07-13  
> 硬件平台：CH582F（RISC-V rv32imac） · 后续支持 CH592 等 WCH BLE MCU

---

## 1. 项目概述

### 1.1 项目背景

at-node 是一个基于 WCH CH582F（RISC-V BLE 5.0 MCU）的 IoT 固件项目。它通过 AT 命令桥接键盘输入、传感器读取和 I/O 控制，可无缝嵌入 AI Agent 管线，实现对电脑的基本远程操作——唤醒、输入文本、按快捷键，或读取环境数据。

项目定位为**低成本、低功耗的物理层 AI Agent 控制节点**，通过有线（USB）和无线（BLE）两种方式模拟键盘，通过串口/虚拟串口接收 AI Agent 的指令。

### 1.2 目标

| 目标 | 说明 |
|------|------|
| **低成本** | 使用国产 WCH BLE 芯片，BOM 成本控制在最低 |
| **低功耗** | 支持深度睡眠，电池供电可长期待机 |
| **双模键盘** | 同时支持蓝牙（BLE HID）和有线（USB HID）键盘 |
| **AT 命令控制** | 通过简单的文本协议控制所有功能 |
| **AI Agent 接入** | 通过串口/虚拟串口与 LLM 管线对接 |
| **多芯片支持** | 硬件抽象层设计，适配 CH582/CH592 等 WCH MCU |

### 1.3 适用场景

- 远程唤醒电脑（WoL / USB 键盘唤醒）
- AI Agent 通过物理键盘输入执行自动化操作
- IoT 传感器数据采集（I²C、ADC）
- 硬件调试与测试（GPIO 控制、串口交互）

---

## 2. 总体架构

### 2.1 系统分层

```
┌──────────────────────────────────────────────┐
│                  应用层（APP）                  │
│  ┌───────────┐ ┌──────────┐ ┌────────────┐  │
│  │ AT 命令解析 │ │ HID 键盘 │ │ 传感器/IO  │  │
│  │   + 路由   │ │  管理    │ │   管理     │  │
│  └─────┬─────┘ └────┬─────┘ └──────┬─────┘  │
├────────┼────────────┼──────────────┼────────┤
│        │    HAL（硬件抽象层）        │        │
│  ┌─────┴─────┐ ┌────┴────┐ ┌──────┴──────┐ │
│  │ KEY / LED │ │  RTC    │ │  MCU / SLEEP│ │
│  └───────────┘ └─────────┘ └─────────────┘ │
├──────────────────────────────────────────────┤
│          StdPeriphDriver（外设驱动）           │
│  GPIO  UART  I²C  SPI  ADC  PWM  USB  Flash  │
├──────────────────────────────────────────────┤
│          BLE Stack（蓝牙协议栈）               │
│         libCH58xBLE.a (LL/HCI/GAP/GATT)       │
├──────────────────────────────────────────────┤
│          RVMSIS + Startup（内核层）            │
│          PFIC/NVIC 中断 · 启动向量             │
└──────────────────────────────────────────────┘
```

### 2.2 任务调度

基于 TMOS（TI 风格的协作式调度器），单循环轮询：

| 任务 | 注册位置 | 职责 |
|------|---------|------|
| HAL 任务 | `HAL_Init()` → `HAL_ProcessEvent` | 按键轮询、LED 控制、休眠管理、定时校准 |
| HID Dev 任务 | `HidDev_Init()` → `hidDev_ProcessEvent` | HID 设备状态机、电池上报 |
| HID 键盘任务 | `HidEmu_Init()` → `HidEmu_ProcessEvent` | 键盘报告生成、连接管理 |
| AT 任务 | **待开发** | AT 命令接收与分发 |

---

## 3. 功能需求

### 3.1 BLE 蓝牙键盘（✅ 已实现）

| 编号 | 需求 | 优先级 | 状态 |
|------|------|--------|------|
| F1.1 | 设备以 BLE Peripheral 角色广播，设备名 "Martillo-T00" | P0 | ✅ |
| F1.2 | 广播数据包含 HID 服务 UUID（0x1812）和电池服务 UUID（0x180F） | P0 | ✅ |
| F1.3 | 连接后发送 8 字节 HID 键盘输入报告（修饰键 + 6 键值） | P0 | ✅ |
| F1.4 | 支持连接参数更新（间隔 10ms，延迟 0，超时 5s） | P0 | ✅ |
| F1.5 | 支持 PHY 更新到 LE 2M | P1 | ✅ |
| F1.6 | 支持绑定（Bonding）与配对 | P1 | ✅ |
| F1.7 | 支持 HID 空闲超时断开（默认 60s） | P2 | ✅ |
| F1.8 | 支持从机连接延时（Slave Latency）以降低功耗 | P2 | ✅ |
| F1.9 | 通过 AT 命令发送任意 HID 键值（非当前单一 F1） | P0 | 🚧 |

### 3.2 USB 有线键盘（🚧 计划中）

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F2.1 | USB Device 模式，枚举为 HID 键盘设备 | P1 |
| F2.2 | 通过 USB HID Report 协议发送按键 | P1 |
| F2.3 | 支持与 BLE 键盘同时工作（双模） | P2 |
| F2.4 | USB 总线供电检测，插入时自动切换为有线模式 | P2 |

### 3.3 AT 命令接口（🚧 计划中）

#### 3.3.1 通用协议

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F3.1 | 基于文本行的请求/响应协议，`\r\n` 作为分隔符 | P0 |
| F3.2 | 命令格式：`AT+<CMD>[=<param1>,<param2>,...]` | P0 |
| F3.3 | 响应格式：`\r\n<结果>\r\n`，成功返回 `OK`，失败返回 `ERROR:<原因>` | P0 |
| F3.4 | 支持 UART（物理串口）和 USB CDC（虚拟串口）两个通道同时接收 | P1 |
| F3.5 | 命令缓冲区 256 字节，支持退格编辑和回显 | P2 |

#### 3.3.2 AT 命令集

| 命令 | 功能 | 参数 | 优先级 |
|------|------|------|--------|
| `AT` | 握手测试（返回 OK） | 无 | P0 |
| `AT+RST` | 软件复位设备 | 无 | P1 |
| `AT+VER` | 查询固件版本和 BLE 协议栈版本 | 无 | P1 |
| `AT+KEY=<keycode>` | 发送单个 HID 按键（按下+释放） | keycode: 十进制/十六进制 HID Usage ID | P0 |
| `AT+KEY_DOWN=<keycode>` | 按键按下 | keycode: HID Usage ID | P1 |
| `AT+KEY_UP=<keycode>` | 按键释放 | keycode: HID Usage ID | P1 |
| `AT+KEY_STR=<text>` | 发送字符串（自动转换为按键序列） | text: ASCII 字符串 | P0 |
| `AT+MODIFIER=<mask>` | 设置修饰键（Ctrl/Alt/Shift/Win） | mask: 8 位位掩码 | P1 |
| `AT+COMB=<k1>,<k2>,...` | 发送组合键（最多 6 键 + 修饰键） | k1~k6: HID Usage ID | P1 |
| `AT+GPIO_W=<pin>,<level>` | 设置 GPIO 输出电平 | pin: 引脚号, level: 0/1 | P1 |
| `AT+GPIO_R=<pin>` | 读取 GPIO 输入电平 | pin: 引脚号 | P1 |
| `AT+ADC=<ch>` | 读取指定通道 ADC 值 | ch: ADC 通道号 | P1 |
| `AT+I2C_SCAN` | 扫描 I²C 总线上的设备 | 无 | P1 |
| `AT+I2C_R=<addr>,<reg>,<len>` | 读取 I²C 设备寄存器 | addr: 7 位地址, reg: 寄存器, len: 字节数 | P1 |
| `AT+I2C_W=<addr>,<reg>,<data>` | 写入 I²C 设备寄存器 | addr, reg, data: 十六进制 | P1 |
| `AT+SLEEP=<mode>` | 进入指定休眠模式 | mode: 0=空闲, 1=睡眠, 2=下电 | P2 |
| `AT+STATUS` | 查询设备状态（连接、电量、信号等） | 无 | P2 |
| `AT+BT_SCAN` | 扫描并列出附近 BLE 设备 | 无 | P3 |
| `AT+WOL=<mac>` | 发送 Wake-on-LAN Magic Packet | mac: 目标 MAC 地址 | P3 |
| `AT+ISP` | 进入 ISP 固件升级模式（写标志 → 复位 → IAP） | 无 | P1 |
| `AT+ISP=USB` | 进入 USB IAP 升级模式 | 无 | P1 |
| `AT+ISP=UART` | 进入 UART IAP 升级模式 | 无 | P1 |

#### 3.3.4 帮助文档（AT+HELP 输出）

`AT+HELP` 应输出以下内容的格式化版本，按功能分组、带简要说明和示例：

```
╔══════════════════════════════════════════════════════════╗
║                at-node AT Command Help                   ║
╚══════════════════════════════════════════════════════════╝

─── 基础命令 ──────────────────────────────────────────────
  AT                 握手测试                    → OK
  AT+HELP [=CMD]     显示帮助（可指定命令）       → 帮助文本
  AT+RST             软件复位设备                → OK
  AT+VER             固件版本与 BLE 协议栈版本    → VER:x.x BLE:x.x
  AT+STATUS          设备状态                    → 连接、电量、信号等
  AT+SLEEP=<mode>    进入休眠（0=空闲/1=睡眠/2=下电）→ OK

─── 键盘命令 ──────────────────────────────────────────────
  AT+KEY=<kc>        发送按键（按下+释放）       → OK
  AT+KEY_DOWN=<kc>   按键按下                    → OK
  AT+KEY_UP=<kc>     按键释放                    → OK
  AT+KEY_STR=<text>  发送字符串                  → OK
  AT+MODIFIER=<mask> 设置修饰键（位掩码）        → OK
  AT+COMB=<k1,..>    发送组合键（最多 6 键）     → OK

  <kc>       = HID Usage ID（十进制或 0x 十六进制）
  <mask>     = 8bit: 1=Ctrl 2=Shift 4=Alt 8=Win
  <text>     = ASCII 字符串，支持空格和常见符号

─── 传感器与 I/O ──────────────────────────────────────────
  AT+GPIO_W=<pin>,<level>   GPIO 输出           → OK
  AT+GPIO_R=<pin>           GPIO 输入读取        → <level>
  AT+ADC=<ch>               ADC 采样             → <value>mV
  AT+I2C_SCAN               I²C 总线扫描         → 设备列表
  AT+I2C_R=<addr>,<reg>,<len>  I²C 寄存器读取    → <data>
  AT+I2C_W=<addr>,<reg>,<data> I²C 寄存器写入    → OK

─── 固件升级 ──────────────────────────────────────────────
  AT+ISP              进入 ISP 升级模式          → OK
  AT+ISP=USB          进入 USB IAP 升级          → OK
  AT+ISP=UART         进入 UART IAP 升级          → OK

─── 无线与网络 ────────────────────────────────────────────
  AT+BT_SCAN          BLE 设备扫描               → 设备列表
  AT+WOL=<mac>        发送 Wake-on-LAN 魔法包    → OK

─── 示例 ──────────────────────────────────────────────────
  AT+KEY_STR=hello           # 输入 hello
  AT+MODIFIER=2+KEY=0x1C     # Ctrl+Enter（注意修饰键需单独设置）
  AT+ISP                     # 切换到固件升级模式
  AT+HELP=KEY                # 查看键盘命令帮助
```

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F3.9 | `AT+HELP` 输出格式化的帮助文本，按功能分组，包含每个命令的用法、参数说明和示例 | P0 |
| F3.10 | `AT+HELP=<CMD>` 仅显示指定命令的详细帮助，不输出全部 | P1 |
| F3.11 | 帮助文本存储在 Flash 只读区，不占用 RAM | P2 |

#### 3.3.5 自动报告（URC）

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F3.6 | 蓝牙连接/断开时自动上报 `+BT_CONNECTED` / `+BT_DISCONNECTED` | P1 |
| F3.7 | GPIO 输入变化时自动上报 `+GPIO_IRQ:<pin>,<level>`（若开启中断） | P2 |
| F3.8 | 电池电量低时自动上报 `+BAT_LOW:<percent>` | P2 |

### 3.4 UART 串口（✅ 已实现基础，🚧 待扩展）

| 编号 | 需求 | 优先级 | 状态 |
|------|------|--------|------|
| F4.1 | UART1 调试输出（TX），115200-8N1 | P0 | ✅ |
| F4.2 | UART1 收发（RX + TX），支持 AT 命令输入 | P0 | 🚧 |
| F4.3 | 支持波特率配置：9600 ~ 115200 | P2 | 🚧 |
| F4.4 | 支持硬件流控（RTS/CTS）可选 | P3 | 🚧 |

### 3.5 USB 虚拟串口（🚧 计划中）

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F5.1 | USB CDC ACM 设备，枚举为虚拟 COM 口 | P1 |
| F5.2 | 虚拟串口与物理串口可同时工作，AT 命令共享 | P1 |
| F5.3 | 支持通过虚拟串口进行固件升级（DFU） | P2 |

### 3.6 GPIO 控制（🚧 计划中）

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F6.1 | 通过 AT 命令配置引脚为推挽输出 / 浮空输入 / 上拉输入 | P1 |
| F6.2 | 通过 AT 命令读取引脚数字电平 | P1 |
| F6.3 | 通过 AT 命令设置引脚输出高/低 | P1 |
| F6.4 | 支持引脚变化中断，自动上报 | P2 |

### 3.7 ADC 采样（🚧 计划中）

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F7.1 | 支持外部单通道 ADC 采样（当前已实现内部温度采样） | P1 |
| F7.2 | 通过 AT 命令指定通道并返回 mV 值 | P1 |
| F7.3 | 支持电池电压采样（`ADC_InterBATSampInit` 已就绪） | P2 |

### 3.8 I²C 传感器扫描（🚧 计划中）

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F8.1 | I²C 主机模式，标准模式 100kHz / 快速模式 400kHz 可配 | P1 |
| F8.2 | 扫描 7 位地址 0x00~0x7F，返回有应答的设备列表 | P1 |
| F8.3 | 读取指定设备的指定寄存器（1~N 字节） | P1 |
| F8.4 | 写入指定设备的指定寄存器 | P1 |

### 3.9 远程唤醒（🚧 计划中）

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F9.1 | 通过 BLE 发送 Magic Packet | P2 |
| F9.2 | 被主机连接时模拟键盘按键唤醒睡眠中的电脑 | P1 |
| F9.3 | USB 连接时通过 USB 唤醒信号唤醒主机 | P2 |

### 3.10 固件升级 IAP（🚧 计划中）

#### 3.10.1 概述

芯片内置 Bootloader，但本项目的固件升级方案采用 **用户自定义 IAP**（In-Application Programming），运行在用户 Flash 中，不依赖芯片 ROM 中的下载协议。参考代码位于 `EVT/EXAM/IAP/`。

Flash 分区布局（以备份升级方案为例）：

| 区域 | 起始地址 | 大小 | 说明 |
|------|---------|------|------|
| JumpIAP（用户引导） | 0x00000000 | 4 KB | 启动跳转判断 |
| ImageA（用户代码） | 0x00001000 | 216 KB | 主固件区 |
| ImageB（备份代码） | 0x00037000 | 216 KB | OTA 备份区（可选，非备份方案可省略） |
| ImageIAP（IAP） | 0x0006D000 | 12 KB | IAP 升级程序 |

#### 3.10.2 升级通道

| 编号 | 通道 | 传输方式 | 状态 |
|------|------|---------|------|
| F10.1 | **USB IAP** | USB 自定义设备（VID=0x4348, PID=0x55E0），BULK 端点 2 传输，上层配合 PC 工具 `WCHMcuIAP_WinAPP` | 🚧 |
| F10.2 | **UART IAP** | UART1 串口，115200-8N1，自定义帧协议 `AA 55 CMD LEN ADDR(4B) DATA(N) CHK(2B) 55 AA`，查询模式 | 🚧 |
| F10.3 | **BLE OTA** | 通过 BLE GATT 服务（UUID=0xFEE0/0xFEE1）无线传输固件 | 🚧 |

#### 3.10.3 IAP 命令协议

三种通道共用同一套 IAP 命令语义：

| 命令码 | 命令 | 功能 |
|--------|------|------|
| 0x80 | `CMD_IAP_PROM` | 编程写入 Flash，满 256 字节才执行写入 |
| 0x81 | `CMD_IAP_ERASE` | 擦除 APP 区 |
| 0x82 | `CMD_IAP_VERIFY` | 校验已写入数据 |
| 0x83 | `CMD_IAP_END` | 结束升级 → 跳转 APP |
| 0x84 | `CMD_IAP_INFO` | 查询固件信息（仅 BLE OTA） |

#### 3.10.4 AT 命令触发 IAP

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F10.4 | 收到 `AT+ISP` 时写 DataFlash 标志位，记录升级通道类型（USB/UART） | P1 |
| F10.5 | 写标志后调用 `SYS_ResetExecute()` 软件复位 | P1 |
| F10.6 | IAP 启动时读取 DataFlash 标志，若为 IAP 模式则停留在 IAP 等待固件，否则直接跳转 APP | P1 |
| F10.7 | IAP 检测超时（USB 1 分钟 / UART 120 秒无操作）自动跳转 APP | P1 |
| F10.8 | IAP 完成后清除标志位，下次复位正常进入 APP | P1 |

#### 3.10.5 上位机工具

| 编号 | 需求 | 优先级 |
|------|------|--------|
| F10.9 | PC 端升级工具（参考 `WCHMcuIAP_WinAPP`），支持 USB IAP 和 UART IAP | P1 |
| F10.10 | 支持从 HEX 文件解析固件并自动发送到设备 | P2 |
| F10.11 | 命令行工具，方便集成到 CI/CD 或 AI Agent 自动化流程 | P3 |

---

## 4. 硬件需求

### 4.1 当前平台（CH582F）

| 参数 | 值 |
|------|-----|
| MCU | CH582F（RISC-V rv32imac，60 MHz） |
| Flash | 448 KB |
| RAM | 32 KB |
| BLE | 4.2/5.0 |
| 外设 | GPIO × 38、I²C × 1、SPI × 2、ADC 14ch × 10bit、PWM × 14、USB 2.0 FS |
| 封装 | QFN48 / QFN32 |

### 4.2 规划平台（CH592）

| 参数 | 值 |
|------|-----|
| MCU | CH592（RISC-V） |
| Flash/RAM | 更大/更灵活的可选配 |
| BLE | 5.4 |
| 功耗 | 更低（先进工艺） |
| 成本 | 更低 |
| 兼容性 | 引脚兼容 CH582，外设 API 全兼容 |

### 4.3 引脚分配

| 引脚 | 功能 | 备注 |
|------|------|------|
| PA8 | LED1 | 推挽输出，低电平亮 |
| PB22 | KEY1 | 上拉输入，低电平触发 |
| PB4 | KEY2 | 上拉输入，低电平触发 |
| PA4 (TXD1) | UART1 TX | 调试输出 / AT 命令响应 |
| PA5 (RXD1) | UART1 RX | **待分配** · AT 命令接收 |
| — | USB D+ / D- | **待分配** · USB HID / USB CDC |
| — | I²C SCL / SDA | **待分配** · 传感器总线 |
| — | ADC_INx | **待分配** · 外部模拟输入 |

---

## 5. 接口需求

### 5.1 物理接口

| 接口 | 类型 | 用途 |
|------|------|------|
| UART1 | 3.3V TTL（115200-8N1） | 调试与 AT 命令 |
| USB 2.0 FS | Device + Host | 有线键盘（Device）+ 虚拟串口（CDC） |
| BLE 4.2/5.0 | 无线 | 蓝牙键盘 |
| I²C | 3.3V 电平 | 传感器读取 |
| GPIO | 3.3V 电平 | 开关量控制、外设触发 |
| ADC | 0~VDD | 模拟量采集 |

### 5.2 AI Agent 对接

```
AI Agent (LLM / Python 脚本)
    │
    ├── 物理串口 ──→ UART TTL ──→ at-node
    │                           │
    ├── USB ──→ USB CDC (虚拟串口) ──→ at-node
    │                                    │
    │                           ┌────────┴────────┐
    │                           │ 键盘（BLE+USB）  │ ←── 对被控电脑
    │                           │ 传感器采集       │
    │                           │ GPIO 控制        │
    │                           └─────────────────┘
```

AI Agent 只需通过串口发送 `AT+KEY_STR=hello` 即可完成物理键盘输入，无需操作系统层面的输入法注入或驱动支持。

---

## 6. 性能需求

| 编号 | 需求 | 指标 | 优先级 |
|------|------|------|--------|
| P1.1 | AT 命令响应时间 | < 10ms（本地命令）/ < 50ms（BLE 命令）| P1 |
| P1.2 | 键盘按键延迟（BLE） | < 20ms（连接态 10ms 间隔）| P1 |
| P1.3 | 键盘按键延迟（USB） | < 5ms（USB 1ms 轮询） | P1 |
| P1.4 | 待机功耗 | < 50μA（睡眠模式）| P2 |
| P1.5 | 广播功耗 | < 500μA（广播间隔 100ms）| P2 |
| P1.6 | 连续工作时长（500mAh 电池） | > 30 天（待机） / > 8 小时（持续按键）| P2 |
| P1.7 | RAM 占用 | < 8 KB（AT 任务 + 缓冲区）| P2 |
| P1.8 | Flash 占用 | < 128 KB（全部功能）| P1 |

---

## 7. 低功耗测试需求

> 本项目的低功耗特性需通过实际硬件测量来验证和标定，以评估 CH582F 芯片的极限功耗能力以及电池供电下的实际待机时长。

### 7.1 测试目标

| 编号 | 目标 | 说明 |
|------|------|------|
| T1.1 | 测定 CH582F **芯片级极限最低功耗** | 在最小系统（仅供电 + 去耦电容）下测量 MCU 各休眠模式的电流 |
| T1.2 | 测定 **系统级极限最低功耗** | 在实际 PCB（含 LED、按键上拉、LDO 等外围）下测量各模式电流 |
| T1.3 | 计算并验证 **电池待机时长** | 基于实测功耗推算，并在实际电池供电下做长时间待机验证 |

### 7.2 测试模式与场景

| 编号 | 模式 | 芯片状态 | 外设状态 | 说明 |
|------|------|---------|---------|------|
| T2.1 | **深度睡眠（下电模式）** | 仅 RTC 运行，CPU 断电，SRAM 保持或断电可选 | 所有外设时钟关闭，GPIO 保持或高阻 | 寻找到达芯片手册标称的极限值 |
| T2.2 | **睡眠模式（Sleep）** | CPU 暂停，SRAM 保持，RTC 运行 | 外设时钟关闭，唤醒后可立即恢复 | 可定时唤醒轮询场景 |
| T2.3 | **空闲模式（Idle）** | CPU 暂停，SRAM 保持 | 外设保持状态，任意中断唤醒 | 最低延迟唤醒场景 |
| T2.4 | **BLE 广播（待连接）** | CPU 间歇工作 | BLE 射频定时广播 | 设备未被连接时的典型功耗 |
| T2.5 | **BLE 连接（空闲）** | CPU 间歇工作 | BLE 射频按连接间隔监听 | 已连接但无按键操作 |
| T2.6 | **BLE 连接（持续按键）** | CPU 持续工作 | BLE 射频发送 + GPIO 扫描 | 最耗电的工作场景 |

### 7.3 测试方法

| 编号 | 方法 | 说明 |
|------|------|------|
| T3.1 | **精密万用表 / 源表** | 串联高精度万用表（如 Keysight 34465A、Fluke 8846A）或使用源表（SMU），测量各模式下的平均电流 |
| T3.2 | **串联电阻 + 示波器** | 在电源回路串联 10Ω 精密电阻，用示波器抓取电流波形，观察瞬态尖峰和脉冲占空比 |
| T3.3 | **低功耗专用评估板** | 使用 CH582F 最小系统板（无 LED、无额外上拉），排除外围干扰 |
| T3.4 | **实际电池放电测试** | 使用 500mAh 锂电池，记录从满电到关机的时间，与实际任务循环对应 |

### 7.4 待测数据

| 编号 | 数据项 | 说明 |
|------|--------|------|
| T4.1 | 各模式 **平均电流**（μA） | 至少连续采样 1 分钟取均值 |
| T4.2 | 各模式 **峰值电流**（mA） | 重点关注 BLE 射频发射和 Flash 擦写时的瞬态峰值 |
| T4.3 | **深度睡眠唤醒时间**（μs） | 从唤醒事件触发到 CPU 执行第一条指令的延迟 |
| T4.4 | **广播间隔 vs 平均功耗** 曲线 | 在 20ms~1000ms 广播间隔下分别测量 |
| T4.5 | **连接间隔 vs 平均功耗** 曲线 | 在 7.5ms~100ms 连接间隔下分别测量 |
| T4.6 | **电池放电曲线** | 记录电压从 4.2V 下降到 2.8V 的过程 |

### 7.5 预期指标（参考芯片数据手册）

| 参数 | CH582F 手册标称 | 实测目标 |
|------|----------------|---------|
| 下电模式（Shutdown） | 0.1 μA | < 0.5 μA（含外围漏电） |
| 睡眠模式（Sleep） | 0.6 μA（32K RTC 运行） | < 2 μA（系统级） |
| 空闲模式（Idle） | — | < 100 μA |
| 广播平均（100ms 间隔） | — | < 200 μA |
| 连接平均（10ms 间隔） | — | < 500 μA |
| 发射峰值（0dBm） | 5.2 mA（发射瞬态） | < 6 mA |

> 注：实测目标为系统级值（包含 LDO 静态功耗、外围漏电等），高于芯片手册纯 MCU 值属正常。

### 7.6 电池待机测算

以实测的深度睡眠功耗为基准，分场景估算：

| 场景 | 假设条件 | 待机时长（500mAh） |
|------|---------|------------------|
| 纯睡眠（下电模式） | 1 μA | **~57 年**（实际受电池自放电和寿命限制） |
| 睡眠 + 每日唤醒 1 次按键 | 平均 2 μA | **~28 年** |
| BLE 广播（1s 间隔，待连接） | 50 μA | **~417 天** |
| BLE 广播（100ms 间隔，待连接） | 200 μA | **~104 天** |
| BLE 连接 + 按键 1h/天 | 脉冲平均 300 μA | **~69 天** |
| BLE 持续按键 | 3 mA | **~7 天** |

> 以上为理论推算值，需通过实际测试修订。

### 7.7 测试报告输出

完成低功耗测试后需输出：

| 编号 | 交付物 | 说明 |
|------|--------|------|
| T5.1 | **功耗测量数据表** | 各模式、各配置下的电流测量记录（Excel/CSV） |
| T5.2 | **电流波形截图** | 示波器抓取的典型工作脉冲波形 |
| T5.3 | **电池放电曲线图** | 500mAh 电池的完整放电过程 |
| T5.4 | **待机时长结论** | 在典型使用场景下的实测待机天数 |
| T5.5 | **功耗优化建议** | 针对非理想因素（外围漏电、软件轮询频率等）的改进方案 |

---

## 8. 非功能需求

### 8.1 可靠性

| 编号 | 需求 | 优先级 |
|------|------|--------|
| Q1.1 | 看门狗定时器，异常时自动复位 | P1 |
| Q1.2 | AT 命令异常输入不会导致设备崩溃 | P1 |
| Q1.3 | BLE 断连后自动重连/广播 | P1 |
| Q1.4 | Flash 参数区磨损均衡 | P2 |

### 8.2 可移植性

| 编号 | 需求 | 优先级 |
|------|------|--------|
| Q2.1 | HAL 层隔离底层芯片差异，上层不直接操作寄存器 | P1 |
| Q2.2 | 芯片型号统一在 `CONFIG.h` 中通过 `CHIP_ID` 宏切换 | P1 |
| Q2.3 | AT 命令解析器与硬件无关，可独立测试 | P2 |

### 8.3 安全

| 编号 | 需求 | 优先级 |
|------|------|--------|
| Q3.1 | 键盘模拟功能必须显式声明其安全风险 | P0 |
| Q3.2 | AT 命令接口应可配置为仅接受特定物理接口 | P1 |
| Q3.3 | 配对绑定信息加密存储于 SNV | P1 |

---

## 9. 代码结构规划

```
software/APP/                    ← 应用层（已有 + 新增）
├── hidkbd_main.c                # 主函数入口（已有）
├── hidkbd.c                     # BLE HID 键盘逻辑（已有）
├── include/hidkbd.h             # HID 键盘头文件（已有）
├── at_parser.c                  # AT 命令解析器（🚧 新增）
├── at_parser.h                  # AT 解析器头文件（🚧 新增）
├── at_cmd_keyboard.c            # 键盘类 AT 命令处理（🚧 新增）
├── at_cmd_sensor.c              # 传感器/IO 类 AT 命令处理（🚧 新增）
├── at_cmd_system.c              # 系统类 AT 命令处理（🚧 新增）
├── usb_hid_kbd.c                # USB 有线键盘（🚧 新增）
├── usb_cdc.c                    # USB 虚拟串口（🚧 新增）
├── iap_main.c                   # IAP 主入口、标志判断与跳转（🚧 新增）
├── iap_usb.c                    # USB IAP 传输层（🚧 新增）
├── iap_uart.c                   # UART IAP 传输层（🚧 新增）
├── iap_flash.c                  # IAP Flash 擦写/校验逻辑（🚧 新增）
└── iap.h                        # IAP 命令定义与共用头文件（🚧 新增）

software/HAL/                    ← 硬件抽象层（已有）
├── KEY.c / KEY.h                # 按键轮询（已有）
├── LED.c / LED.h                # LED 控制（已有）
├── MCU.c                        # MCU 初始化、温度校准（已有）
├── RTC.c / RTC.h                # RTC 定时（已有）
├── SLEEP.c / SLEEP.h            # 休眠管理（已有）
├── I2C.c / I2C.h                # I²C 高层封装（🚧 新增）
├── ADC.c / ADC.h                # ADC 高层封装（🚧 新增）
├── GPIO.c / GPIO.h              # GPIO 高层封装（🚧 新增）
└── include/CONFIG.h             # 全局配置宏（已有）
```

---

## 10. 开发路线图

| 阶段 | 内容 | 预计 |
|------|------|------|
| **v0.1** | BLE HID 键盘基础功能（当前状态） | ✅ 已完成 |
| **v0.2** | AT 命令解析器 + 键盘命令 + UART 收发 | 🚧 进行中 |
| **v0.3** | USB HID 键盘 + USB CDC 虚拟串口 + USB IAP 固件升级 | 🚧 规划 |
| **v0.4** | GPIO/ADC/I²C 命令 + 传感器管理 + UART IAP | 🚧 规划 |
| **v0.5** | CH592 移植 + 低功耗测试与优化 | 🚧 规划 |

---

## 11. 附录

### 11.1 参考文档

- [CH582 数据手册](https://www.wch.cn/products/CH582.html)
- [CH583 EVT 参考代码](../EVT/)
- [HID Usage Tables (USB-IF)](https://www.usb.org/hid)
- [BLE HID over GATT Profile Spec](https://www.bluetooth.com/specifications/specs/hid-over-gatt-profile/)

### 11.2 术语

| 术语 | 说明 |
|------|------|
| TMOS | TI 风格的协作式任务调度器 |
| HID | Human Interface Device，人机交互设备 |
| GATT | Generic Attribute Profile，BLE 属性协议 |
| CDC | Communication Device Class，USB 通信设备类 |
| URC | Unsolicited Result Code，主动上报消息 |
| SNV | 简易非易失存储（Flash 模拟 EEPROM） |
| IAP | In-Application Programming，在应用编程（运行在用户 Flash 中的升级程序） |
| ISP | In-System Programming，系统内编程（芯片 Bootloader 模式，通过串口/USB 下载固件） |
