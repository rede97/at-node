# TEST TODO — 待硬件环境的功能测试清单

> 版本：v0.1 · 2026-07-22
> 用法：新 PCB / 仪器到位后按表执行；每项标注依赖、步骤、通过判据。
> 图例：⬜ 未测 | 🚧 部分/冒烟已验 | ✅ 通过

---

## A. 外设驱动（M5，需新 PCB / 附件）

| # | 测试 | 依赖 | 步骤 | 判据 | 状态 |
|---|------|------|------|------|------|
| A1 | GPIO 输出 | 万用表/LED | `AT+GPIO_W=12,1` / `=12,0`(PA12) | 引脚电平 3.3V/0V | ⬜ |
| A2 | GPIO 回环 | 杜邦线 PA12→PA13 | `AT+GPIO_W=12,x` 后 `AT+GPIO_R=13` | 读到与写入一致（注意读=上拉输入模式） | ⬜ |
| A3 | GPIO 输入 | 按键/短路到 GND | `AT+GPIO_R=<pin>` 高低各一次 | 读值随外部电平翻转 | ⬜ |
| A4 | ADC 定压 | 稳压源/分压 1.65V | `AT+ADC=<ch>` | 读数 1650mV ±5%（超差校准 `HWS_ADC_FULLSCALE_MV`) | ⬜ |
| A5 | I²C 扫描 | 任意 I²C 设备（EEPROM/传感器） | `AT+I2C_SCAN` | 列出设备 7 位地址 | ⬜ |
| A6 | I²C 读写 | 同上（如 24Cxx @0x50) | `AT+I2C_W=0x50,0x00,0xA5` → `AT+I2C_R=0x50,0x00,1` | 读回 A5 | ⬜ |
| A7 | IR 载波/时序 | 示波器（PA12) | `AT+IR=RAW,9000,4500,560,560` | 载波 37.5kHz±1kHz,mark/space 时序 ±5% | ⬜ |
| A8 | IR NEC 实机 | 空调/电视 | `AT+IR=NEC,0x807F00FF`（换真实码） | 设备响应 | ⬜ |
| A9 | IR SIRC/RAW | 示波器/设备 | `AT+IR=SIRC,0xA90,12` | 波形/响应 | ⬜ |

## B. 低功耗（§7，需电流表/示波器）

| # | 测试 | 依赖 | 步骤 | 判据 | 状态 |
|---|------|------|------|------|------|
| B1 | 各模式电流 | 精密万用表/SMU | 下电/睡眠/空闲/广播/连接/连按 六模式各测 1 分钟均值 | 对照 §7.5 预期指标出数据表 | ⬜ |
| B2 | 峰值电流 | 示波器+10Ω 串阻 | BLE 发射/Flash 擦写瞬态 | < 6mA | ⬜ |
| B3 | 广播/连接间隔-功耗曲线 | 同上 | 20ms~1s 广播、7.5~100ms 连接间隔各测 | 出曲线图（§7 T4.4/4.5) | ⬜ |
| B4 | 电池放电 | 500mAh 锂电 | 满电→2.8V 记录 | 放电曲线 + 待机天数结论 | ⬜ |
| B5 | `AT+SLEEP` 实装验证 | 电流表 | stub 实装后测睡眠进入/唤醒 | 电流落到 B1 对应档位 | ⬜（功能未实现） |

## C. BLE 链路健壮性

| # | 测试 | 依赖 | 步骤 | 判据 | 状态 |
|---|------|------|------|------|------|
| C1 | 监督超时 | 双板 | dongle 连 kbd 后**断电** kbd 板 | dongle ≤10s 打印 `+BT_DISC: reason`（验证 conn_handle 修复后 LL 超时是否触发） | ✅（reason 0x08,<20s,已固化 `test_features.py`) |
| C2 | 反向监督超时 | 双板 | 断开 dongle USB（或复位） | kbd 板 ≤10s `BLE=disconnected` 且恢复广播 | 🚧 **开放**:Peripheral 侧 LL 超时 60s+ 不触发（Central 侧正常），疑栈层问题，列阶段三 |
| C3 | LSI 时钟影响 | 外 32.768k 晶振（如有） | `-DCLK_OSC32K=0` 构建对比 C1/C2 | 外晶振下超时行为 | ⬜ |
| C4 | 长稳 soak | 双板过夜 | 连接挂 12h+，定时按键转发 | 无掉线/无内存异常 | ⬜ |
| C5 | 绑定失配退避 | 双板 | kbd `AT+BT_PAIR` 单侧擦除 | dongle 5 次失败自动 hold,AT 不淹没（已验 07-21) | ✅ |
| C6 | RPA 重连（RK) | RK 键盘 | F1.22 实施后 | 地址旋转下自动回连 | ⬜（暂缓） |
| C7 | RPA 重连（C3 模拟） | ESP32-C3(阶段六②) | C3 周期换地址,dongle `AT+BT_AUTO=1` | 地址旋转下自动回连成功 | ⬜（等 C3 sketch,**先于 RK**) |

## D. 主机交互

| # | 测试 | 依赖 | 步骤 | 判据 | 状态 |
|---|------|------|------|------|------|
| D1 | 键盘唤醒主机 | PC 睡眠（允许键盘唤醒） | kbd 连接 PC,PC 睡眠后 `AT+TAP=50,0,44`(空格） | PC 唤醒（F9.2) | ⬜ |
| D2 | USB 键盘唤醒 | 同上，USB 连接 | 主机睡眠，`AT+TAP` 经 USB HID | PC 唤醒（F9.3) | ⬜ |
| D3 | 手机/平板配对 | Android/iOS | 配对 AT-Node kbd，`AT+KEY_STR=hi` | 字符出现在手机输入框 | ⬜ |
| D4 | UART1 物理 AT | USB-TTL 线接 PA4/PA5 | 串口工具发 `AT`/`AT+KEY_STR` | 与 CDC 同等响应（F4.2) | ⬜ |
| D5 | 多主机并发（F1.10–15) | 3 台主机 | 功能实现后：绑定 3 台，`AT+DEV` 切换发键 | 各主机独立收到对应按键 | ⬜（功能未实现） |

## E. 固件工程

| # | 测试 | 依赖 | 步骤 | 判据 | 状态 |
|---|------|------|------|------|------|
| E1 | 看门狗复测 | 双板 | `WWDG_ENABLE=TRUE` 构建烧录，VMware 拔插 USB | CDC 异步后不再复位循环；故意死循环（测试钩子）能 0.5s 复位 | ⬜ |
| E2 | CDC 异步背压 | 拔插主机 USB | 掉线时连续 `AT+BT_SCAN` 刷屏后回插 | 无复位；`cdc_tx_drops` 计数增长（DIAG)；回插后输出恢复 | 🚧（实现已验编译，待实测） |
| E3 | ISP 调试锁 | — | 文档记录：wchisp 烧录后 `CFG_DEBUG_EN=Disable`，两线调试关闭 | 烧录流程避开 ISP（dongle 只走 wlink)/找到解锁方法 | 🚧（已记录，待解） |
| E4 | dual 角色再验证 | dual.hex | `AT+ROLE` 双向切换（新代码基线） | 同 M4 判据 | ⬜ |
| E5 | Flash 占用预算 | — | dual 构建功能继续增长 | 关注 44.95% → 60% 阈值，超则裁剪 | 🚧（持续） |

## F. RK 全量键盘（F1.22,P4 暂缓）

| # | 测试 | 依赖 | 步骤 | 判据 | 状态 |
|---|------|------|------|------|------|
| F1 | Report Map 解析器 | 331B dump（已存） | 宿主侧单测先行 | 5 个 Report ID 分类正确 | ⬜ |
| F2 | RK 实机全链路 | RK-S75RGB | 配对→订阅→NKRO 打字 | 按键正确出现在主机 | ⬜ |
| F3 | C3 黄金键盘 | ESP32-C3(阶段六③) | 手工 Report Map(NKRO ID1+boot8 ID2+消费控制 ID4) | F1.22 分派逻辑验收 | ⬜（F1.22 陪练） |

---

### 已通过（归档）

- M1–M4 全部里程碑（句柄修复/dongle 硬化/Linux CI/角色切换）
- 双板 loop + hardening 全 PASS（多轮）
- AT 回归 6/6、AT+TAP、URC、AT+STATUS、AT+HELP=<CMD>、AT+KEY_STR 冒烟
- ISP 链路（kbd 板，VMware 注意事项见 SKILL §5/§7)
- 看门狗复位循环根因定位（CDC 阻塞 + 0.56s 硬狗）→ CDC TMOS 异步修复
